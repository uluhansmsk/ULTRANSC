[project]
name = "ultransc"
version = "0.7.0b1"
description = "Local-first transcription pipeline for long lecture batches."
requires-python = ">=3.9"

[tool.ultransc]
entrypoint = "ultransc.cli:main"
